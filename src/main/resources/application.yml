spring:
  application:
    name: libraryApi
  datasource:
    url: jdbc:postgresql://localhost:5432/library
    password: postgres
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        jdbc:
          lob:
            non-contextual-creation: true


